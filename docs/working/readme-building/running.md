# Running Commissions

<!-- STUB. Question this page answers: what controls and visibility do
     I get when I run a tree, and what must I hold in my head to run
     one well? The operator's page. -->

<!-- Opening: run_commission is the only door in, dispatch the only
     path around inside; controls are set once and nothing in the tree
     can route around them. The trust line ("hand a tree real money and
     real tools, walk away") opens or closes this page. -->

## One Run, One Set of Controls

<!-- Moved whole from the current README: budget-with-fuse, resource
     fuses and halt semantics, tool ceiling, concurrency room, model
     catalog. -->

## Observability

<!-- Moved whole: watch (logging) / react (on_progress, on_llm_call,
     on_dispatch) / query (records, calls + dispatches tables), the
     always-on dispatch register. -->

## What You Must Actually Hold

<!-- Moved whole: the honest five-item list (underscore vocabulary,
     meanings of None, three words for tool restriction, money's three
     dialects, persistence precedence). This is operator knowledge, so
     it lives on the operator's page. -->

## Where to Go Next

<!-- concepts -> commission-model.md, build -> authoring.md,
     prove -> commission-testing.md -->
