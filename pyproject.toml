[project]
name = "vibrantine"
version = "0.3.0"
description = "A component model for AI agents: typed, contracted, isolated units of LLM-driven work."
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Joshua Weir" }]
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2",
    "httpx",
    "openai",
]

[project.urls]
Repository = "https://github.com/vibrantine/vibrantine"

[dependency-groups]
dev = [
    "pytest",
    "pytest-asyncio",
    # Pinned exactly: the formatter's style can shift between releases, and
    # a silent upgrade would re-flag clean files in CI. Upgrading is a
    # deliberate act: bump the pin, run `ruff format .`, commit both.
    "ruff==0.15.13",
    "basedpyright",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/vibrantine"]
exclude = [
    "src/vibrantine/**/tests",
    "src/vibrantine/**/tests/**",
]
artifacts = [
    "src/vibrantine/**/prompts/*.md",
]

[tool.pytest.ini_options]
testpaths = ["tests", "src/vibrantine/examples"]
asyncio_mode = "auto"
markers = [
    "integration: tests that hit real external services; skip without credentials.",
    "eval: graded live-model efficacy runs; skip without credentials.",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]

[tool.basedpyright]
include = ["src", "tests"]
pythonVersion = "3.12"
typeCheckingMode = "strict"
